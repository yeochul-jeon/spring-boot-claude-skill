# 회고 #13 — Boot 4.x 최소 Gradle 버전 오기재 (결함 H)

날짜: 2026-04-22

---

## 사건 요약

회고 #12 반영 후 `test-retro11/order-api/build.gradle.kts` 에
`id("io.spring.dependency-management") version "1.1.7"` 을 추가하고
`./gradlew build --no-daemon` 를 실행했다. 그러나 빌드가 다시 실패했다:

```
> Failed to apply plugin 'org.springframework.boot'.
   > Spring Boot plugin requires Gradle 8.x (8.14 or later) or 9.x.
     The current version is Gradle 8.13
BUILD FAILED in 7s
```

wrapper 는 회고 #12 재검증 로그에서 `gradle wrapper --gradle-version 8.13` 으로
생성되었고, 참조 문서도 "Boot 4.x → Gradle 8.4+" 로 기재되어 있어 올바른 버전으로
인식되었다. 그러나 Spring Boot 4.0.5 플러그인은 실제로 8.14 이상을 요구한다.

---

## 발견된 결함

### 결함 H — Boot 4.x 최소 Gradle 버전 오기재

- 기존 문서: "Boot 4.x → Gradle 8.4+" (4개 파일, 5개 위치)
- 실제 요구사항 (empirical): Boot 4.0.5 플러그인 오류 메시지에서 확인
  `Spring Boot plugin requires Gradle 8.x (8.14 or later) or 9.x`
- 영향: fallback scaffold 및 wrapper 갱신 시 8.4~8.13 을 지정해도 문서상 허용이나
  실제 빌드 실패. 회고 #11~12 의 "빌드 가능성" 판정이 이 결함으로 계속 미성립.

---

## 수정 파일

| 파일 | 변경 내용 |
|------|-----------|
| `skills/spring-init/references/gradle-conventions.md` | §0 표 `8.4+` → `8.14+ 또는 9.x`, 예시 커맨드 `8.14.3` 으로 갱신, staleness 경고 추가 |
| `skills/spring-init/SKILL.md` | §2-c `8.4` → `8.14+`, 오류 메시지 기반 재지정 지시 추가; §3-a `8.4 이상` → `8.14+ 또는 9.x` |
| `tests/regression-11-fallback-buildability.md` | 결함 D 체크리스트 `8.4+` → `8.14+` |

---

## 교훈

1. **정적 호환표는 Boot 패치 릴리스 경계에서 stale 된다.**
   "Boot 4.x" 와 같이 major.x 로 범위를 잡은 표는 패치 릴리스(4.0.0 → 4.0.5 등)에서
   내부 요구사항이 조용히 상향될 수 있다. 테이블보다 **빌드 실패 로그의 오류 메시지**가
   더 신뢰 가능한 근거다.

2. **wrapper 생성 직후 `./gradlew help` 로 플러그인 적용 성공을 확인해야 한다.**
   wrapper 생성 자체는 성공해도 bootVersion 플러그인이 Gradle 버전 요건을 충족하지
   않으면 모든 gradlew 명령이 실패한다. wrapper 생성 → `./gradlew help` 성공 →
   이후 단계 진행을 표준 순서로 고정한다.

3. **`Spring Boot plugin requires Gradle ...` 오류 메시지를 파싱해 재지정한다.**
   이 메시지는 요구 Gradle 버전을 명시적으로 포함한다. 문서상 버전보다 이 메시지를
   우선해 `gradle wrapper --gradle-version <요구값>` 을 재실행하면 된다.

---

## 재검증 — 테스트 B (중단 프로토콜)

날짜: 2026-04-22 (retro13 수정 반영 후 동일 일자 재검증)

`tests/regression-11-fallback-buildability.md` §3 을 `/etc/hosts` 에
`127.0.0.1 start.spring.io` 차단 상태에서 실행.

### 체크리스트 결과

- ✅ 3회 재시도 로그 (1차 즉시 → sleep 2 → sleep 5)
- ✅ metadata 접근 실패 및 중단 명시, "학습 데이터 버전 추정 금지" 원칙 인용
- ✅ `build.gradle.kts` 등 산출물 0건
- ✅ "버전 숫자를 직접 알려주시면 진행 가능" 류 우회 제안 0건
- ✅ 대안 제시 — `/etc/hosts` 차단 해제 후 재실행 안내
- ✅ 무단 복구 금지 — "hosts 차단 이유가 있으시면 말씀해주세요" 로 사용자 의도 확인

### 양호한 추가 행동

재시도 3회 실패 후 `curl -sv` 로 metadata API 를 직접 호출해
`IPv4: 127.0.0.1` 응답을 확인 → `/etc/hosts` 라인 195 를 원인으로 정확히 특정.
단순 "네트워크 실패" 보고에 머무르지 않고 근본 원인까지 진단한 점은
SKILL §2-d "대안: 네트워크 복구 … 사용자 승인" 지침을 적극적으로 이행한 사례.

### 미세 개선 제안 (결함은 아님)

복구 안내로 `sudo sed -i '' '195s/^/# /' /etc/hosts` 를 제시했으나
라인 번호는 환경 의존적이다. `tests/regression-11-fallback-buildability.md` §3 의
패턴 기반 형식이 더 안전하다:

```bash
sudo sed -i '' '/start.spring.io/d' /etc/hosts
```

이는 회고 #14 로 승격할 정도는 아니며, 본 로그에 기록만 남긴다.

---

## 최종 판정

테스트 A (fallback → 빌드 성공) + 테스트 B (중단 프로토콜) 모두 통과.
회고 #11~13 의 "fallback 산출물 빌드 가능성" 및 "중단 원칙 준수" 목표가
empirical 하게 성립했다.

# 작업 로그 — 2026-04-21

## 참고 자료

- `PLAN.md` (설계 A–E)
- `PLAN-implementation.md` (구현 F–J)
- `CLAUDE.md` (프로젝트 절대 규칙)

---

## [Sprint 1] spring-init 스킬 구축

### 작업 내역

#### Step 0 — 문서 스캐폴드 ✅

- `docs/ADR.md` 생성 (ADR-001~003)
- `docs/logs/log-20260421.md` 생성 (이 파일)

#### Step 1 — 디렉터리 생성 ✅

- `skills/spring-init/references/`
- `skills/spring-init/scripts/`
- `tests/`

#### Step 2 — `references/decision-tree.md` 작성 ✅

PLAN-implementation.md G-1 전문 이식.

#### Step 3 — scripts 3종 작성 + chmod +x ✅

- `fetch-latest-versions.sh`
- `generate-project.sh`
- `apply-package-structure.sh`

전부 `-rwxr-xr-x` 권한 확인.

#### Step 4 — `fetch-latest-versions.sh` 실행 검증 ✅

실행 시각: 2026-04-21 (로컬 KST 기준)

반환 JSON:
```json
{
  "bootVersion": "4.0.5.RELEASE",
  "javaDefault": "17",
  "javaAvailable": ["26","25","21","17"]
}
```

**관찰**:
- Spring Boot 4.0.5.RELEASE가 현재 `start.spring.io`의 기본값.
- `.RELEASE` 접미사가 붙어서 반환됨 — API가 주는 문자열 그대로 사용.
- Java 기본은 17이지만 21이 사용 가능 → CLAUDE.md 스택(Java 21 LTS)과 일치.
- **이 버전 숫자는 스킬/코드/문서 본문에 절대 기록하지 않는다** (케이스 1 리그레션 규칙).

#### Step 5 — `SKILL.md` 작성 ✅

PLAN-implementation.md F-1 전문 이식.
- YAML frontmatter (`name: spring-init`, `description` 트리거 패턴)
- 절대 원칙 3개 (버전 하드코딩 금지, 아키텍처 가정 금지, spring-principles 체크리스트)
- 워크플로우 6단계 (인터뷰 → 버전 조회 → 프로젝트 생성 → 패키지 구조 → YAML → 원칙 검증)
- 사후 체크리스트 (버전 숫자 없음, Java 21 toolchain, .gitignore, 프로파일, smoke test)
- Boot 버전 숫자 미포함 확인 ✅

#### Step 6 — `references/` 5개 파일 작성 ✅

| 파일 | 내용 |
|---|---|
| `starter-api.md` | GET /metadata/client + POST /starter.zip 파라미터표, dependency id 매핑 |
| `architecture-styles.md` | Layered/Hexagonal/Clean 언제·왜·장단점·탈출 신호 |
| `package-structure.md` | 스타일별 디렉터리 트리 3종 + 공통 권장 사항 |
| `gradle-conventions.md` | Kotlin DSL 플러그인 순서, Java 21 toolchain, libs.versions.toml 중앙화 |
| `application-yml.md` | 환경별 프로파일 분리, ${ENV_VAR} 참조 패턴, .gitignore 추가 권장 |

#### Step 7 — `tests/cases.md` 작성 + 케이스 1, 2 실행 ✅

`tests/cases.md` 생성 — 5개 케이스 전문 기록.

**케이스 1 실행 결과**:
```bash
grep -rE '(springBoot|spring.boot|org\.springframework\.boot)[^#]*[0-9]+\.[0-9]+\.[0-9]+' skills/spring-init/
# → PASS: Spring Boot 버전 하드코딩 없음
```
- testcontainers/mapstruct 서드파티 버전은 의도적 → PASS 처리
- 검증 항목 전부 ✅

**케이스 2 실행 결과**:
- `SKILL.md` 워크플로우 Step 1에 `decision-tree.md` 참조 명시 ✅
- `apply-package-structure.sh`가 `layered|hexagonal|clean` 분기 구현 ✅
- 기본값 hardcode 없음 (스타일은 항상 인터뷰 후 결정) ✅

---

#### Step 8 — 최종 점검 ✅

**신규 ADR 없음** — 실행 중 설계 변경이 없어 ADR-001~003 그대로 유지.

**최종 생성 파일 목록** (13개):

```
docs/
├── ADR.md
└── logs/log-20260421.md

skills/spring-init/
├── SKILL.md
├── references/
│   ├── decision-tree.md
│   ├── starter-api.md
│   ├── architecture-styles.md
│   ├── package-structure.md
│   ├── gradle-conventions.md
│   └── application-yml.md
└── scripts/
    ├── fetch-latest-versions.sh      (chmod +x)
    ├── generate-project.sh           (chmod +x)
    └── apply-package-structure.sh    (chmod +x)

tests/
└── cases.md
```

**End-to-End 검증 결과**:

| 항목 | 결과 |
|---|---|
| SKILL.md + references/ 6개 + scripts/ 3개 존재 | ✅ |
| 스크립트 3종 `-rwxr-xr-x` 권한 | ✅ |
| `fetch-latest-versions.sh` 실제 network 호출 성공 | ✅ |
| Spring Boot 버전 하드코딩 grep → PASS | ✅ |
| 케이스 1 (버전 하드코딩 방지) | PASS |
| 케이스 2 (아키텍처 인터뷰 구조) | PASS |

---

### 다음 스킬 예고

**`spring-principles`** — 다른 스킬들이 참조하는 토비의 스프링 기반 체크리스트.
별도 세션/플랜으로 착수. 생성자 주입, DTO-Entity 분리, Rich domain, `@Transactional` 경계 등 자가 검증 규칙을 정의한다.

---

### Git commit 대기

CLAUDE.md 규칙: "통과 시 Git commit → 다음 스킬".
사용자 확인 후 `feat: spring-init 스킬 추가` 커밋 예정 (자동 커밋 안 함).
